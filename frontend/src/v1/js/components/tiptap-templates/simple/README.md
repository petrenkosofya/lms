# Simple Editor - Professional Tiptap-based Rich Text Editor

Профессиональный WYSIWYG редактор на базе Tiptap 3.x с расширенным функционалом, заменяющий устаревший UberEditor (EpicEditor).

## Возможности

### Форматирование текста
- ✨ **Bold, Italic, Underline, Strikethrough** - полный набор форматирования
- 📝 **Headings** (H1-H6) - структурирование контента
- 🎨 **Text Alignment** - выравнивание по левому краю, центру, правому краю

### Контент
- 🔗 **Links** - добавление и редактирование ссылок с удобным UI
- 🖼️ **Images** - вставка изображений по URL
- 📋 **Lists** - маркированные и нумерованные списки
- 💬 **Blockquotes** - цитаты
- 💻 **Code blocks** - блоки кода с подсветкой синтаксиса

### Управление
- ↶ **Undo/Redo** - история изменений
- 💾 **Автоматическая синхронизация** с textarea для форм
- ⚡ **React-based** - современная архитектура
- 🚀 **Простой API** для интеграции
- 📱 **Responsive** - адаптивный дизайн для мобильных устройств

## Использование

### В HTML (автоматическая инициализация)

Все `<textarea>` с классом `ubereditor` автоматически заменяются на SimpleEditor:

```html
<textarea class="ubereditor" name="content">
Начальное содержимое
</textarea>
```

### Программная инициализация

```javascript
import { initSimpleEditor } from 'components/tiptap-templates/simple';

const textarea = document.querySelector('textarea');
const editor = initSimpleEditor(textarea);

// API
editor.getContent();  // Получить содержимое
editor.setContent('<p>Новый текст</p>');  // Установить содержимое
editor.focus();  // Фокус на редакторе
editor.clear();  // Очистить содержимое
editor.destroy();  // Уничтожить редактор
```

### Использование как React компонента

```jsx
import { SimpleEditor } from '@/components/tiptap-templates/simple/simple-editor';

export default function App() {
  return <SimpleEditor />
}
```

С управляемым состоянием:

```jsx
import { SimpleEditor } from '@/components/tiptap-templates/simple/simple-editor';

function MyComponent() {
  const [content, setContent] = useState('');

  return (
    <SimpleEditor 
      content={content}
      onChange={(newContent) => setContent(newContent)}
      placeholder="Начните печатать..."
    />
  );
}
```

## Toolbar кнопки

### Форматирование
- **B** - Жирный текст (Ctrl+B)
- **I** - Курсив (Ctrl+I)
- **U** - Подчеркнутый текст (Ctrl+U)
- **S** - Зачеркнутый текст

### Структура
- **H1, H2, H3** - Заголовки первого, второго и третьего уровней

### Выравнивание
- **⇤** - Выравнивание по левому краю
- **⇥** - Выравнивание по центру
- **⇥** - Выравнивание по правому краю

### Списки и блоки
- **• List** - Маркированный список
- **1. List** - Нумерованный список
- **</>** - Блок кода
- **"** - Цитата (blockquote)

### Медиа и ссылки
- **🔗** - Добавить/редактировать ссылку (открывает поле ввода URL)
- **🖼️** - Вставить изображение (по URL)

### Прочее
- **—** - Горизонтальная линия
- **↶** - Отменить (Ctrl+Z)
- **↷** - Повторить (Ctrl+Shift+Z)

## Установленные расширения

Редактор использует следующие официальные Tiptap расширения:

- `@tiptap/starter-kit` - базовый набор (paragraph, bold, italic, strike, etc.)
- `@tiptap/extension-link` - поддержка ссылок
- `@tiptap/extension-underline` - подчеркивание текста
- `@tiptap/extension-text-align` - выравнивание текста
- `@tiptap/extension-placeholder` - placeholder для пустого редактора
- `@tiptap/extension-image` - вставка изображений

## Кастомизация

### Добавление новых расширений

Отредактируйте `simple-editor.tsx`:

```typescript
import { useEditor } from '@tiptap/react';
import StarterKit from '@tiptap/starter-kit';
import Table from '@tiptap/extension-table';

const editor = useEditor({
  extensions: [
    StarterKit,
    Table.configure({
      resizable: true,
    }),
    // ... другие расширения
  ],
  // ...
});
```

### Кастомизация стилей

Отредактируйте `simple-editor.scss`:

```scss
.simple-editor-wrapper {
  .tiptap-editor {
    // Ваши стили
  }
}
```

## Примеры использования

Смотрите файл `example.tsx` для полных примеров:
- Базовое использование
- Контролируемое состояние
- Интеграция с формами
- Работа с API редактора

## Техническая информация

- **React**: 18.3.1
- **Tiptap Core**: 3.10.5
- **Tiptap Extensions**: 
  - `@tiptap/react` 3.10.5
  - `@tiptap/starter-kit` 3.10.5
  - `@tiptap/extension-link` 3.10.5
  - `@tiptap/extension-underline` 3.10.5
  - `@tiptap/extension-text-align` 3.10.5
  - `@tiptap/extension-placeholder` 3.10.5
  - `@tiptap/extension-image` 3.10.5
- **TypeScript**: 5.7.3

## Поддержка браузеров

- Chrome (последние 2 версии)
- Firefox (последние 2 версии)
- Safari (последние 2 версии)
- Edge (последние 2 версии)

## Troubleshooting

### Редактор не инициализируется

Убедитесь, что:
1. React и ReactDOM загружены
2. Textarea существует в DOM
3. Нет JavaScript ошибок в консоли

### Содержимое не синхронизируется с формой

Проверьте:
1. Textarea передан в параметре `textarea`
2. Форма не отправляется до завершения инициализации

### Стили не применяются

Убедитесь, что:
1. SCSS файл импортирован
2. Webpack правильно обрабатывает SCSS
3. Нет конфликтов с другими стилями

## Лицензия

ISC
