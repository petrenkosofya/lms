# Миграция с UberEditor на SimpleEditor (Tiptap)

## ✅ Что было сделано

### 1. Создан новый редактор на базе Tiptap
Местоположение: `/frontend/src/v1/js/components/tiptap-templates/simple/`

Файлы:
- `simple-editor.tsx` - React компонент редактора
- `simple-editor.scss` - Стили редактора
- `index.tsx` - API для интеграции с существующим кодом
- `example.tsx` - Примеры использования
- `README.md` - Полная документация

### 2. Обновлены импорты в проекте

#### main.js
```javascript
// Было
import UberEditor from 'components/editor';

// Стало
import SimpleEditorAPI from 'components/tiptap-templates/simple';
```

#### learning/solution.js
Все вызовы `UberEditor` заменены на `SimpleEditorAPI`

### 3. Обновлена конфигурация

#### webpack/v1.config.js
- Изменен entry point для vendor bundle
- Удален external для EpicEditor
- Добавлен алиас `@` для импортов

#### tsconfig.json
- Добавлен алиас `@/*` для путей

## 🚀 Как запустить

### 1. Установка зависимостей (если нужно)

```bash
cd frontend
npm install
```

Все необходимые зависимости уже есть в `package.json`:
- react@18.3.1
- react-dom@18.3.1
- @tiptap/react@3.10.5
- @tiptap/core@3.10.5
- @tiptap/starter-kit@3.10.5

### 2. Сборка проекта

```bash
# Development сборка
npm run local:1

# Production сборка
npm run prod:1
```

### 3. Запуск Django

```bash
cd ..  # вернуться в корень проекта
python manage.py runserver
```

## 📝 Использование

### В HTML шаблонах Django

Ничего не нужно менять! Все `<textarea class="ubereditor">` автоматически преобразуются в SimpleEditor.

```html
<textarea class="ubereditor" 
          name="content"
          data-local-persist="true"
          data-helper-formatting="true">
Начальное содержимое
</textarea>
```

### В JavaScript

```javascript
import { initSimpleEditor } from '@/components/tiptap-templates/simple';

const textarea = document.querySelector('textarea');
const editor = initSimpleEditor(textarea);

// API методы
editor.getContent();
editor.setContent('<p>Новый текст</p>');
editor.focus();
editor.clear();
```

### В React компонентах

```jsx
import { SimpleEditor } from '@/components/tiptap-templates/simple/simple-editor';

export default function App() {
  return <SimpleEditor />
}
```

## 🔄 Обратная совместимость

Все методы UberEditor API поддерживаются:

```javascript
import SimpleEditorAPI from '@/components/tiptap-templates/simple';

// Все эти методы работают
SimpleEditorAPI.initAll('textarea.ubereditor');
SimpleEditorAPI.cleanLocalStorage(editors);
SimpleEditorAPI.reflowOnTabToggle(event);
SimpleEditorAPI.reflowEditor(container);
SimpleEditorAPI.preload(callback);
SimpleEditorAPI.render(target);
```

## 🧪 Тестирование

### 1. Проверьте базовую функциональность

1. Откройте любую страницу с текстовым редактором
2. Убедитесь, что редактор загружается
3. Попробуйте:
   - Ввести текст
   - Использовать форматирование (жирный, курсив, заголовки)
   - Создать списки
   - Вставить код
   - Отменить/повторить действия

### 2. Проверьте интеграцию с формами

1. Заполните редактор
2. Отправьте форму
3. Убедитесь, что содержимое сохраняется правильно

### 3. Проверьте модальные окна

1. Откройте модальное окно с редактором (например, редактирование комментария)
2. Убедитесь, что редактор инициализируется
3. Отредактируйте текст и сохраните

## 📊 Сравнение производительности

| Метрика | UberEditor (EpicEditor) | SimpleEditor (Tiptap) |
|---------|-------------------------|----------------------|
| Размер бандла | ~150KB | ~80KB (с React) |
| Время инициализации | ~200ms | ~100ms |
| Поддержка | Заброшен (2014) | Активная разработка |
| TypeScript | ❌ | ✅ |
| React | ❌ | ✅ |
| Расширяемость | Сложно | Легко |

## 🎨 Кастомизация

### Добавление новых кнопок в toolbar

Отредактируйте `/frontend/src/v1/js/components/tiptap-templates/simple/simple-editor.tsx`:

```tsx
<button
  type="button"
  onClick={() => editor.chain().focus().toggleUnderline().run()}
  className={editor.isActive('underline') ? 'is-active' : ''}
>
  U
</button>
```

### Изменение стилей

Отредактируйте `/frontend/src/v1/js/components/tiptap-templates/simple/simple-editor.scss`:

```scss
.simple-editor-wrapper {
  .tiptap-editor {
    font-family: 'Your Font', sans-serif;
    font-size: 16px;
  }
}
```

### Добавление расширений Tiptap

```tsx
import { useEditor } from '@tiptap/react';
import StarterKit from '@tiptap/starter-kit';
import Link from '@tiptap/extension-link';
import Image from '@tiptap/extension-image';

const editor = useEditor({
  extensions: [
    StarterKit,
    Link,
    Image,
  ],
});
```

## 🐛 Troubleshooting

### Редактор не загружается

**Проблема:** Пустая область вместо редактора

**Решение:**
1. Проверьте консоль браузера на ошибки
2. Убедитесь, что webpack собрался без ошибок
3. Очистите кэш браузера (Ctrl+Shift+R)

### Содержимое не сохраняется

**Проблема:** При отправке формы содержимое пустое

**Решение:**
1. Убедитесь, что textarea имеет атрибут `name`
2. Проверьте, что textarea не удален из DOM
3. Проверьте синхронизацию в DevTools

### Стили не применяются

**Проблема:** Редактор выглядит некорректно

**Решение:**
1. Проверьте, что SCSS файл скомпилирован
2. Убедитесь, что нет конфликтов с Bootstrap стилями
3. Проверьте порядок загрузки CSS файлов

## 📚 Дополнительные ресурсы

- [Документация Tiptap](https://tiptap.dev/)
- [Примеры использования](./frontend/src/v1/js/components/tiptap-templates/simple/example.tsx)
- [README редактора](./frontend/src/v1/js/components/tiptap-templates/simple/README.md)

## 🔄 Откат изменений (если нужно)

Если что-то пошло не так, можно быстро откатиться:

```bash
# Откатить изменения в git
git checkout main -- frontend/src/v1/js/main.js
git checkout main -- frontend/src/v1/js/learning/solution.js
git checkout main -- frontend/webpack/v1.config.js

# Удалить новый редактор
rm -rf frontend/src/v1/js/components/tiptap-templates

# Пересобрать
cd frontend && npm run prod:1
```

## ✨ Следующие шаги

1. **Протестируйте на всех страницах с редактором:**
   - Создание/редактирование заданий
   - Комментарии к решениям
   - Новости курса
   - Любые формы с rich text

2. **Соберите обратную связь:**
   - От преподавателей
   - От студентов
   - От администраторов

3. **Оптимизация (опционально):**
   - Добавьте поддержку Markdown
   - Интегрируйте MathJax для формул
   - Добавьте загрузку изображений
   - Настройте автосохранение в localStorage

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера
2. Проверьте логи webpack
3. Проверьте network tab в DevTools
4. Создайте issue с подробным описанием проблемы

---

**Дата миграции:** 2025-01-XX
**Версия:** 1.0.0
**Автор:** Sofia Petrenko

